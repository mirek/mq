---
name: Automate releases
description: Add reproducible release CI and a generated changelog policy.
---

# Automate releases

Define versioning, provenance, package ordering, tag creation, and changelog
generation in CI with least-privilege publishing credentials.

Acceptance: a dry run verifies artifacts and release metadata deterministically.
