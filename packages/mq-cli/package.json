{
  "name": "@prelude/mq-cli",
  "version": "0.0.0",
  "description": "Command-line interface for @prelude/mq.",
  "type": "module",
  "files": ["dist"],
  "bin": {
    "mq": "./dist/cli.js"
  },
  "engines": {
    "node": ">=24.0.0"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/mirek/mq.git",
    "directory": "packages/mq-cli"
  },
  "scripts": {
    "build": "tsc --build tsconfig.build.json",
    "clean": "rm -rf dist *.tsbuildinfo",
    "test": "node --test",
    "typecheck": "tsc --build"
  },
  "dependencies": {
    "@prelude/mq": "workspace:*"
  },
  "publishConfig": {
    "access": "public",
    "provenance": true
  }
}
